#include<iostream>
#include<cstdio>
#include<iomanip>
#include<algorithm>
#include<cstring>
#include<math.h>
#include<cctype>
#define LL long long
#define LD long double
using namespace std;
int main(){
    LL A,B,C;
    cin>>A>>B>>C;
    if(A>B)swap(A,B);
    if(B>C)swap(B,C);
    if(C<A)swap(A,C);
    if(A>B)swap(A,B);
    if(B>C)swap(B,C);
    if(C<A)swap(A,C);
    if(A>B)swap(A,B);
    if(B>C)swap(B,C);
    if(C<A)swap(A,C);
    getchar();
    char temp;
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
    temp=getchar();
    if (temp=='A')cout<<A<<' ';
    if (temp=='B')cout<<B<<' ';
    if (temp=='C')cout<<C<<' ';
}
